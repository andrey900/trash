<?if (!defined("B_PROLOG_INCLUDED") || B_PROLOG_INCLUDED!==true)die();

/*$fn = 'arrFilter';
if( $arParams['FILTER_NAME'] )
	$fn = $arParams['FILTER_NAME'];

$fn = $GLOBALS[$fn];

foreach ($fn as $prop => $val) {
	if( strpos($prop, "ROPERTY_BRAND") && isset($arResult['ITEMS'][6]['VALUES'][$val]) ){
		$arResult['ITEMS'][6]['VALUES'][$val]['CHECKED'] = "Y";
	}
}*/